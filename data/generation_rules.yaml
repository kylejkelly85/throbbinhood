model: "phi3:mini"
temperature: 0.8  # Slight bump for more descriptive flow
top_p: 0.9
repeat_penalty: 1.02  # Dropped significantly

story_length:
  min_words: 1500  # More realistic target for a single-pass 3B JSON property
  max_words: 3000

required_sections:
  - summary
  - story

required_metadata:
  - title
  - genre
  - tropes
  - series
  - source_story

max_tropes_per_story: 4